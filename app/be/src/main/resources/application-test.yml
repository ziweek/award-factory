spring:
  config:
    activate:
      on-profile: test

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:awardFactoryH2DB
    username: admin
    password: admin